server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
#        server-addr: localhost:8848
        server-addr: 172.51.101.99:1111

management:
  endpoints:
    web:
      exposure:
        include: '*'